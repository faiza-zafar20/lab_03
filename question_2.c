#include<stdio.h>

int main() {
    int num1;
    int num2;
    printf("the first num is: ");
    scanf("%d", &num1);
    printf("the first num is:%d\n", num1);
    printf("the second num is: ");
    scanf("%d", &num2);
    printf("the second num is:%d\n", num2); return 0;
}
